#include <string>
using namespace std;

int solution(string A, string B) {
    if(A == B) return 0;
    
    for(int i=1; i<(int)A.size(); i++){
        A = string(1, A[A.size()-1]) + A.substr(0, A.size()-1);
        if(A == B) return i;
    }
    
    return -1;
}
